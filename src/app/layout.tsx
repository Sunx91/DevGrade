import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
  display: "swap",
});

export const metadata: Metadata = {
  title: "DevGrade – GitHub Portfolio Analyzer",
  description:
    "Measure the engineering health of your GitHub portfolio. Advanced heuristics for code quality, contribution velocity, and architectural impact.",
  keywords: ["github", "portfolio", "developer", "analysis", "code quality"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body className={`${inter.className} antialiased`}>{children}</body>
    </html>
  );
}
